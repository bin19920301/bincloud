spring:
  application:
    name: test-user
  cloud:
    nacos:
      discovery:
        server‐addr: localhost:8848
        namespace: dev
      config:
        server-addr: localhost:8848
        namespace: dev
        file-extension: yaml